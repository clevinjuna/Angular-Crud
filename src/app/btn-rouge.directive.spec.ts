import { BtnRougeDirective } from './btn-rouge.directive';

describe('BtnRougeDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnRougeDirective();
    expect(directive).toBeTruthy();
  });
});
